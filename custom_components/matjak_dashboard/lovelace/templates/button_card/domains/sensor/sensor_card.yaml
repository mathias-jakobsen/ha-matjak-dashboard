mj_sensor_card:
  template: mj_entity_card

  variables:
    mj_swap_name_state: true

  styles:
    card:
      - --icon-color: >
          [[[
            return `var(--color-sensor-${entity?.attributes?.device_class}, var(--main-color))`;
          ]]]
