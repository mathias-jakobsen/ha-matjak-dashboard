|
  .: |
    ha-app-layout {
      --header-height: 0px;
    }

    app-header {
      display: none;
    }

    #view {
      height: 100vh !important;
    }

    app-drawer {
      box-shadow: var(--sidebar-box-shadow-right);
    }